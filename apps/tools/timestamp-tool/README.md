# 时间戳转换工具

一个轻量的时间戳与日期互转工具。

## 功能
- 时间戳秒 / 毫秒 / 微秒互转
- 可搜索的全球时区列表
- 多时区预览
- 批量时间戳转换
- 当前时间实时更新
- 复制结果
- 响应式布局

## 运行

```bash
cd /root/projects/apps/timestamp-tool && npm install && npm run dev
```

## 构建

```bash
cd /root/projects/apps/timestamp-tool && npm run build
```
